<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.skylark.permission.authorization.mapper.PermissionAuditMapper">

    <insert id="insert">
        INSERT INTO sys_permission_audit(tenant_id, operator, action, detail)
        VALUES (#{row.tenantId}, #{row.operator}, #{row.action}, #{row.detail})
    </insert>

</mapper>

