<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.skylark.permission.authorization.mapper.TenantAdminBindingMapper">

    <resultMap id="BaseResultMap" type="cn.skylark.permission.authorization.entity.SysTenantAdminBinding">
        <id column="tenant_id" property="tenantId" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="role_id" property="roleId" jdbcType="BIGINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="selectByTenantId" resultMap="BaseResultMap">
        SELECT tenant_id, user_id, role_id, create_time, update_time
        FROM sys_tenant_admin_binding
        WHERE tenant_id = #{tenantId}
        LIMIT 1
    </select>

    <insert id="upsert">
        INSERT INTO sys_tenant_admin_binding(tenant_id, user_id, role_id)
        VALUES (#{tenantId}, #{userId}, #{roleId})
        ON DUPLICATE KEY UPDATE
            user_id = VALUES(user_id),
            role_id = VALUES(role_id)
    </insert>

</mapper>

