# 将下面「某一组」的两行复制到项目根目录 `.env`，并去掉行首 #。
# （与 docker-compose.yml 同级；compose 会自动读 `.env` 里的 MAVEN_BUILD_IMAGE / JRE_IMAGE）
#
# 说明：daemon.json 里的 registry-mirrors 只加速 Docker Hub，对 ghcr.io / mcr.microsoft.com 无效。
# ghcr.io 在国内常与 GitHub 同样超时，若出现 “waiting for connection … ghcr.io” 请换组 1 / 4 或配置代理。
#
# --- 组 1：DaoCloud 同步的 Docker Hub library 镜像
#MAVEN_BUILD_IMAGE=docker.m.daocloud.io/library/maven:3.9-eclipse-temurin-17
#JRE_IMAGE=docker.m.daocloud.io/library/eclipse-temurin:17-jre-alpine
#
# --- 组 2：运行时走 GHCR（仅在国际网络或已为 Docker 配置代理时可用）
#MAVEN_BUILD_IMAGE=docker.m.daocloud.io/library/maven:3.9-eclipse-temurin-17
#JRE_IMAGE=ghcr.io/adoptium/eclipse-temurin:17-jre-alpine
#
# --- 组 3：已从任意源拉取到本机后，与 Hub 保持一致命名（避免构建再去解析 docker.io）
# 先在终端执行：
#   docker pull <你可用的镜像>/eclipse-temurin:17-jre-alpine
#   docker tag <同上完整名> eclipse-temurin:17-jre-alpine
# 然后 `.env` 里可以不设 JRE_IMAGE（沿用默认名，使用本地已有镜像）。
#
# --- 组 4：运行时改用微软容器注册表 MCR（与 Docker Hub / GHCR 不同线路，可试）
# 基础系统为 Mariner（非 Alpine），体积略大，运行 Spring Boot 可执行 JAR 无影响。
#MAVEN_BUILD_IMAGE=docker.m.daocloud.io/library/maven:3.9-eclipse-temurin-17
#JRE_IMAGE=mcr.microsoft.com/openjdk/jdk:17-mariner
#
# 若仅浏览器能翻墙：Docker Desktop → Settings → Proxies → Manual，填 http://127.0.0.1:<端口>
#（与 Clash / V2 的「允许局域网连接」HTTP 代理端口一致）；仅开系统 VPN 时 Docker 常仍直连失败。
# 备选：代理软件开 TUN 模式，使本机所有流量走代理后再拉镜像。
